# Simple Node.js Project

A small HTTP server built with Node.js built-in modules.

## Run

```bash
npm start
```

Open http://localhost:3000 in a browser.

For automatic restarts while editing:

```bash
npm run dev
```

Set a different port with `PORT=4000` before starting the server.
